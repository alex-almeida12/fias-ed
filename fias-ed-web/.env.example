# Copie para .env e troque TODAS as senhas (use valores longos e aleatórios,
# só com letras, números, - e _: elas entram nas URLs de conexão do banco).
POSTGRES_PASSWORD=troque-esta-senha-do-superusuario
FIAS_ED_MIGRATOR_PASSWORD=troque-esta-senha-do-migrator
FIAS_ED_APP_PASSWORD=troque-esta-senha-da-aplicacao
DEVICE_ID=fias-ed-web
# MAX_UPLOAD_BYTES e client_max_body_size em deploy/nginx.conf (1536m) mudam JUNTOS;
# depois de mudar o nginx, reconstrua: docker compose up -d --build web
MAX_UPLOAD_BYTES=1610612736
MIN_AUDIO_SECONDS=60
MAX_AUDIO_SECONDS=9000
# Diretório dos pesos (fora do Git). Populado por scripts/setup_models.py.
MODELS_DIR=/models
# Só para a instalação (serviço setup-models, profile "setup"): token de LEITURA do
# Hugging Face, e o diretório dos experimentos de onde o BERTimbau é copiado.
# O produto em execução não usa nenhum dos dois. Nunca versione o valor do token.
HUGGINGFACE_TOKEN=
FIAS_ED_EXPERIMENTS_DIR=../../artigos selecionados/experimentos
# Tamanho do modelo de ASR. `small` foi medido e escolhido: numa aula de 90 min leva
# 0,17x a duração do áudio e 1,14 GB de pico de memória — veja "Escolha do modelo de
# ASR" no README, com a tabela e o critério. `tiny` e `base` têm revisão fixada em
# scripts/setup_models.py só para a medição: adotar um deles exige antes a entrada em
# fias-ed-shared/scientific-config/models.json, senão o carregamento recusa.
ASR_SIZE=small
# Execução offline: nenhuma chamada de rede em tempo de execução.
HF_HUB_OFFLINE=1
TRANSFORMERS_OFFLINE=1
